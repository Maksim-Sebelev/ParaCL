cmake_minimum_required(VERSION 3.30)

project(ParaCL
    VERSION 5.1
)

set(PARACL_SRC_DIR ${PROJECT_SOURCE_DIR})

set(PARACL_SNIPPY_SRC_DIR ${PARACL_SRC_DIR}/Snippy)
set(PARACL_FRONTEND_SRC_DIR ${PARACL_SRC_DIR}/Frontend)
set(PARACL_COMPILER_SRC_DIR ${PARACL_SRC_DIR}/Compiler)

set(PARACL_SUBPROJECTS_BINARY_DIR ${CMAKE_CURRENT_BINARY_DIR}/subprojects)

set(PARACL_SNIPPY_BINARY_DIR ${PARACL_SUBPROJECTS_BINARY_DIR}/Snippy)
set(PARACL_FRONTEND_BINARY_DIR ${PARACL_SUBPROJECTS_BINARY_DIR}/Frontend)
set(PARACL_COMPILER_BINARY_DIR ${PARACL_SUBPROJECTS_BINARY_DIR}/Compiler)

set(PARACL_SNIPPY_EXE paracl-snippy)
set(PARACL_COMPILER_EXE paracl)

include(ExternalProject)


ExternalProject_Add(
    ParaCL-Frontend
    BINARY_DIR ${PARACL_FRONTEND_BINARY_DIR}
    SOURCE_DIR ${PARACL_FRONTEND_SRC_DIR}
    BUILD_COMMAND ${CMAKE_COMMAND} --build <BINARY_DIR>
    CMAKE_ARGS -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER} -DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE} -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
    BUILD_ALWAYS ON
)

ExternalProject_Add(
    ParaCL-Compiler
    BINARY_DIR ${PARACL_COMPILER_BINARY_DIR}
    SOURCE_DIR ${PARACL_COMPILER_SRC_DIR}
    BUILD_COMMAND ${CMAKE_COMMAND} --build <BINARY_DIR>
    INSTALL_COMMAND ${CMAKE_COMMAND} -E copy ${PARACL_COMPILER_BINARY_DIR}/paracl ${CMAKE_CURRENT_BINARY_DIR}/${PARACL_COMPILER_EXE}
    CMAKE_ARGS -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER} -DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE} -DPARACL_FRONT=${PARACL_FRONTEND_BINARY_DIR}/paracl-front -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
    BUILD_ALWAYS ON
)

ExternalProject_Add(
    ParaCL-snippy
    GIT_REPOSITORY https://github.com/Maksim-Sebelev/ParaCL-snippy.git
    GIT_TAG main
    SOURCE_DIR ${PARACL_SNIPPY_SRC_DIR}
    BINARY_DIR ${PARACL_SNIPPY_BINARY_DIR}
    BUILD_COMMAND ${CMAKE_COMMAND} --build <BINARY_DIR>
    INSTALL_COMMAND ${CMAKE_COMMAND} -E copy ${PARACL_SNIPPY_BINARY_DIR}/paracl-snippy ${CMAKE_CURRENT_BINARY_DIR}/${PARACL_SNIPPY_EXE}
    CMAKE_ARGS -DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER} -DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE} -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
    BUILD_ALWAYS OFF
)

# TESTS
include(CTest)
enable_testing()

add_test(
    NAME CompilerTests
    COMMAND ${CMAKE_CTEST_COMMAND} --test-dir ${PARACL_COMPILER_BINARY_DIR}
    LABELS ParaCL-Compiler
)
